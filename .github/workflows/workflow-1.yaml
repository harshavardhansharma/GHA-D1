name: My Workflow
on: workflow_dispatch
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Step-1
              run: echo "This is Step-1 of the first job"
            - name: Print_Goodbye
              run: |
                echo "Goodbye from Step-2 of the first job"
                echo "This is the end of the first job"
                echo "-----------------------------"