name: My Workflow
on: 
  push:
    branches:
      - main
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Step-1
              run: echo "This is Step-1 of the second job"
            - name: Print_Goodbye
              run: |
                echo "Goodbye from Step-2 of the second job"
                echo "This is the end of the second job"
                echo "-----------------------------"